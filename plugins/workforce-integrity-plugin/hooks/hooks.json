{
  "hooks": [
    {
      "name": "auto-verify-file-references",
      "event": "post_tool_call",
      "matcher": {
        "tools": [
          "write_to_file",
          "replace_file_content",
          "multi_replace_file_content"
        ]
      },
      "action": {
        "command": "python3 -c \"import os, sys, subprocess; script = next((p for p in ['.agents/skills/workforce-management/scripts/validate-references.py', 'skills/workforce-management/scripts/validate-references.py'] if os.path.exists(p)), None); subprocess.run([sys.executable, script, './', '--fix']) if script else None\""
      }
    }
  ]
}
