{
  "hooks": [
    {
      "name": "auto-track-token-usage",
      "event": "post_tool_call",
      "matcher": {
        "tools": [
          "*"
        ]
      },
      "action": {
        "command": "python3 -c \"import os, sys, subprocess; script = next((p for p in ['.agents/skills/usage-tracker/scripts/print_turn_summary.py', 'skills/usage-tracker/scripts/print_turn_summary.py'] if os.path.exists(p)), None); subprocess.run([sys.executable, script]) if script else None; script2 = next((p for p in ['.agents/skills/usage-tracker/scripts/track_usage.py', 'skills/usage-tracker/scripts/track_usage.py'] if os.path.exists(p)), None); subprocess.run([sys.executable, script2, './']) if script2 else None\""
      }
    }
  ]
}
