{
  "hooks": [
    {
      "name": "auto-code-graph-symbol-discovery",
      "event": "pre_tool_call",
      "matcher": {
        "tools": [
          "write_to_file",
          "replace_file_content",
          "multi_replace_file_content"
        ]
      },
      "action": {
        "command": "python3 -c \"import os, sys, subprocess; script = next((p for p in ['.agents/skills/code-graph/scripts/graph_indexer.py', 'skills/code-graph/scripts/graph_indexer.py'] if os.path.exists(p)), None); subprocess.run([sys.executable, script, '--scan', './']) if script else None\""
      }
    }
  ]
}
